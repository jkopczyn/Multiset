require 'spec_helper'

describe Multiset do
  it 'has a version number' do
    expect(Multiset::VERSION).not_to be nil
  end

  it 'can create an empty multiset' do
      expect(Multiset.new).to_not raise_error
  end

  it 'can create a multiset from an array' do
      fail
  end

  it 'can create a multiset from a set' do
      fail
  end

  it 'can create a multiset from another multiset' do
      fail
  end

  it 'tests equality correctly' do
      fail
  end

  it 'can add elements' do
      fail
  end

  it 'can remove elements' do
      fail
  end

  it 'cleans elements with zero instances' do
      fail
  end

  it 'can erase all instances of an element' do
      fail
  end

  it 'can iterate over all elements in the set' do
      fail
  end

  it 'can iterate through all element, number pairs' do
      fail
  end

  it 'can give its own size' do
      fail
  end

  it 'can clear itself' do
      fail
  end

  it 'can check whether it is empty' do
      fail
  end

  it 'can convert to an array' do
      fail
  end

  it 'can convert to a set' do
      fail
  end

  it 'can flatten member sets and multisets to the top level' do
      fail
  end

  it 'can test being a subset of a multiset' do
      fail
  end

  it 'can test being a subset of a set' do
      fail
  end

  it 'can test being a superset of a multiset' do
      fail
  end

  it 'can test being a superset of a set' do
      fail
  end

  it 'can merge another collection into the multiset' do
      fail
  end

  it 'can subtract another collection from the multiset' do
      fail
  end

  it 'can filter items for obeying some test' do
      fail
  end
end
